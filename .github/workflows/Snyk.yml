name: Snyk SCA Scan

on:
  push:
    branches: [ Devsecops_Demo ]
  pull_request:
    branches: [ Devsecops_Demo ]

permissions:
  contents: read
  security-events: write

jobs:
  snyk-sca:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Build WebGoat
        run: mvn -B clean package -DskipTests

      - name: Install Snyk CLI
        run: npm install -g snyk

      - name: Install Snyk CLI + HTML converter
        run: npm install -g snyk snyk-to-html

      - name: Run Snyk scan and generate HTML report
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        continue-on-error: true
        run: |
          snyk test --all-projects --json > snyk.json || echo "{}" > snyk.json
          snyk-to-html < snyk.json > snyk-report.html || echo "<html><body>No results</body></html>" > snyk-report.html

      - name: Upload HTML report as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: snyk-html-report
          path: snyk-report.html

      - name: Monitor project in Snyk
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: snyk monitor --all-projects
